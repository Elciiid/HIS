{
 "_note": "Kaggle runs: paired two-stage training on a full-fidelity dataset. n_sims is the pool the sampler draws from; how many actually run is set by --budget-hours from the measured seconds per storm. store_coarsen 1 keeps snapshots at 20 m so one dataset can train a model at any coarser grid (the A1 resolution comparison needs both).",
 "data": {
  "n_sims": 60,
  "coarsen": 2,
  "store_coarsen": 1,
  "compress_records": true
 },
 "model": {
  "two_stage": true
 },
 "train": {
  "steps": 10000,
  "paired": true,
  "effect_floor": 0.5,
  "ckpt_seconds": 600.0
 }
}
